package kt.guide.best_practice

import kotlin.test.Test
import kotlin.test.assertEquals

class KFluentSetterTest {
    // @Test
    // fun searchConfig() {
    //     val root = "~/folder"
    //     val term = "kotlin"
    //     val recursive = true
    //     val followSymlinks = false
    //     val actuals = KSearchConfig(root, term, recursive, followSymlinks)

    //     assertEquals(root, actuals.root)
    //     assertEquals(term, actuals.term)
    //     assertEquals(recursive, actuals.recursive)
    //     assertEquals(followSymlinks, actuals.followSymlinks)
    // }
}